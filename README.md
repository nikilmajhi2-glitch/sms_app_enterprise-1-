SMS Enterprise Project bundle
=============================
This package includes:
- Flutter app (android-only device SMS integration) with modular code.
- Node.js backend with routes, models, Dockerfile, docker-compose, redis.
- React admin panel (skeleton).
- CI config, tests, postman collection.

This is intentionally large to serve as a full starter enterprise scaffold.
See each subfolder README for details.